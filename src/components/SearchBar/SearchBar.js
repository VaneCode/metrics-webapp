// Imports

const SearchBar = () => (
  <div className="search">
    <div className="searchInputs">
      <input
        type="text"
        placeholder="Search"
        value="filter"
      />
    </div>
  </div>
);

export default SearchBar;
